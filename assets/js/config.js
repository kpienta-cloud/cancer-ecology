/* Cancer Ecology Center — site configuration
   Edit this file to configure the live Substack feed. */
window.CEC_CONFIG = {
  /*  TODO: Replace with live Substack RSS feed URL once the publication is created.
      Example: 'https://cancerecologynotes.substack.com/feed'
      The site reads from this URL via rss2json.com when set; otherwise it falls
      back to the v1 placeholder posts defined in placeholder-posts.js. */
  substackFeedUrl: null,
  rss2jsonEndpoint: 'https://api.rss2json.com/v1/api.json',
  contactEmail: 'kpienta1@jhmi.edu',
  githubOrg: 'https://github.com/kpienta-cloud',
  publicationName: 'Cancer Ecology Notes',
};
